# nano-agent

A tiny Claude-Code-like coding agent, built from 0 to 1, one mechanism at a time.

This repository is the subject of the Repo2Learn sample course. It implements a
minimal agent loop with tool use, a system prompt, todo tracking, memory,
skills, and MCP tools — each isolated in a small file so each lesson can quote
real code.
